# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# WhatsApp number used for booking confirmations (local PK format is fine,
# it is normalised to international format before building wa.me links).
VITE_WHATSAPP_NUMBER=03314896544

# Admin panel password (client-side gate only — real protection must come
# from Supabase Row Level Security / Auth). Shared by /admin and /content admin.
VITE_ADMIN_PASSWORD=change-me
